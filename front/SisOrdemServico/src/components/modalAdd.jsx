import React, { useState } from "react";
import axios from "axios";

export function ModalAdd({ isOpen, onClose }) {
    if (!isOpen) return null; 



    return(
        <div>
            
        </div>
    );
}